import {FeignClientBuilder} from "./FeignClientBuilder";
import {FeignClientExecutor} from "./FeignClientExecutor";



export default class DefaultFeignClientBuilder implements FeignClientBuilder {


    build= <T extends FeignClientExecutor>() => {

        return null;
    };








}
